{namespace ClayBadge}

/**
 * This renders a badge.
 */
{template .render}
	{@param label: string}
	{@param? id: string}
	{@param? style: string}

	{let $classes kind="text"}
		badge badge-pill
		{sp}badge-{$style ?: 'primary'}
	{/let}

	<span class="{$classes}" id="{$id}">
		{$label}
	</span>
{/template}