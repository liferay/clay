{namespace Chart}

/**
 * This renders the component's whole content.
 */
{template .render}
	<div class="metal-chart" ref="chart"></div>
{/template}

/**
 * This renders the component's loading graphic.
 */
{template .loading}
	<pre>loading...</pre>
{/template}