{namespace ClayDataProvider}

/**
 * This renders the component's whole content.
 */
{template .render}
	{@param content: any}
	{@param? loadingContent: any}
	{@param? _isLoading: bool}

	{if $_isLoading and $loadingContent}
		{$loadingContent}
	{else}
		{$content}
	{/if}
{/template}