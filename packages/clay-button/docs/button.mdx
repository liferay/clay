---
title: 'Button'
description: 'Buttons communicate an action to happen on user interaction.'
lexiconDefinition: 'https://liferay.design/lexicon/core-components/buttons/'
packageNpm: '@clayui/button'
packageUse: "import Button from '@clayui/button';"
packageTypes:
    [
        'clay-button/src/Button.tsx',
        'clay-button/src/ButtonWithIcon.tsx',
        'clay-button/src/Group.tsx',
    ]
---

## Display Types

You can determine how your button can be displayed using the `displayType` property:

If you want use the button as a link set `displayType` to `link`.

```jsx preview
import {Provider} from '@clayui/core';
import Button from '@clayui/button';
import React from 'react';

import '@clayui/css/lib/css/atlas.css';

export default function App() {
	return (
		<Provider spritemap="/public/icons.svg">
			<div className="p-4">
				<Button.Group spaced>
					<Button displayType="primary">Button Primary</Button>
					<Button displayType="secondary">Button Secondary</Button>
					<Button displayType="link">Button Link</Button>
					<Button borderless>Button Borderless</Button>
				</Button.Group>
			</div>
		</Provider>
	);
}
```

## Outline Borderless

```jsx preview
import {Provider} from '@clayui/core';
import Button from '@clayui/button';
import React from 'react';

import '@clayui/css/lib/css/atlas.css';

export default function App() {
	return (
		<Provider spritemap="/public/icons.svg">
			<div className="p-4">
				<Button
					borderless
					className="mr-2"
					displayType="primary"
					outline
				>
					Primary
				</Button>
				<Button
					borderless
					className="mr-2"
					displayType="secondary"
					outline
				>
					Secondary
				</Button>
			</div>
		</Provider>
	);
}
```

## Outline Borderless Dark

```jsx preview
import {Provider} from '@clayui/core';
import Button from '@clayui/button';
import React from 'react';

import '@clayui/css/lib/css/atlas.css';

export default function App() {
	return (
		<Provider spritemap="/public/icons.svg">
			<div className="bg-dark p-4 ">
				<Button
					borderless
					className="mr-2"
					dark
					displayType="primary"
					outline
				>
					Primary
				</Button>
				<Button
					borderless
					className="mr-2"
					dark
					displayType="secondary"
					outline
				>
					Secondary
				</Button>
			</div>
		</Provider>
	);
}
```

## Translucent

```jsx preview
import {Provider} from '@clayui/core';
import Button from '@clayui/button';
import React from 'react';

import '@clayui/css/lib/css/atlas.css';

export default function App() {
	return (
		<Provider spritemap="/public/icons.svg">
			<div className="p-4">
				<Button className="mr-2" displayType="primary" translucent>
					Primary
				</Button>
				<Button className="mr-2" displayType="secondary" translucent>
					Secondary
				</Button>
				<Button className="mr-2" displayType="info" translucent>
					Info
				</Button>
				<Button className="mr-2" displayType="success" translucent>
					Success
				</Button>
				<Button className="mr-2" displayType="warning" translucent>
					Warning
				</Button>
				<Button className="mr-2" displayType="danger" translucent>
					Danger
				</Button>
			</div>
		</Provider>
	);
}
```

## Translucent Dark

```jsx preview
import {Provider} from '@clayui/core';
import Button from '@clayui/button';
import React from 'react';

import '@clayui/css/lib/css/atlas.css';

export default function App() {
	return (
		<Provider spritemap="/public/icons.svg">
			<div className="bg-dark p-4">
				<Button className="mr-2" dark displayType="primary" translucent>
					Primary
				</Button>
				<Button
					className="mr-2"
					dark
					displayType="secondary"
					translucent
				>
					Secondary
				</Button>
				<Button className="mr-2" dark displayType="info" translucent>
					Info
				</Button>
				<Button className="mr-2" dark displayType="success" translucent>
					Success
				</Button>
				<Button className="mr-2" dark displayType="warning" translucent>
					Warning
				</Button>
				<Button className="mr-2" dark displayType="danger" translucent>
					Danger
				</Button>
			</div>
		</Provider>
	);
}
```

## Group

You can use the variant `Button.Group` for creating button groups:

Use the `spaced` property to create spacing between buttons.

```jsx preview
import {Provider} from '@clayui/core';
import Button from '@clayui/button';
import React from 'react';

import '@clayui/css/lib/css/atlas.css';

export default function App() {
	return (
		<Provider spritemap="/public/icons.svg">
			<div className="p-4">
				<Button.Group>
					<Button>This</Button>
					<Button displayType="secondary">is</Button>
					<Button>a</Button>
					<Button displayType="secondary">button</Button>
					<Button>group.</Button>
				</Button.Group>
			</div>
		</Provider>
	);
}
```

## Icon

You can use the high-level component `ClayButtonWithIcon` to create a button with only an icon. If you need an icon and text, you need to compose with `ClayIcon`

```jsx preview
import {Provider} from '@clayui/core';
import Button, {ClayButtonWithIcon} from '@clayui/button';
import Icon from '@clayui/icon';
import React from 'react';

import '@clayui/css/lib/css/atlas.css';

export default function App() {
	return (
		<Provider spritemap="/public/icons.svg">
			<div className="p-4">
				<ClayButtonWithIcon
					aria-label="Close"
					symbol="times"
					title="Close"
				/>
				<Button>
					<span className="inline-item inline-item-before">
						<Icon symbol="times" />
					</span>
					Close w/ text
				</Button>
			</div>
		</Provider>
	);
}
```
