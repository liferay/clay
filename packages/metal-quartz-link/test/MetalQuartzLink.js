'use strict';

import MetalQuartzLink from '../src/MetalQuartzLink';

let component;

describe('MetalQuartzLink', function() {
	afterEach(() => {
		if (component) {
			component.dispose();
		}
	});
});
