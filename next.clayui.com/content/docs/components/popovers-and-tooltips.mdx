---
title: 'Popover and Tooltip'
description: 'Popovers and Tooltips are patterns used to show helpful information in two different ways.'
packageNpm: '@clayui/popover @clayui/tooltip'
packageVersion: '3.0.0'
packageStatus: 'Stable'
sibling: 'docs/components/css-popovers-and-tooltips.html'
---

import {Popover} from '../../../src/components/clay/Popover';
import {Tooltip, TooltipProvider} from '../../../src/components/clay/Tooltip';

<div class="clay-site-alert alert alert-warning">
	Starting in Clay v3, we have separated popover and tooltip into separate
	packages, `@clayui/popover` and `@clayui/tooltip`.
</div>

<div class="nav-toc-absolute">
<div class="nav-toc">

-   [Popover](#popover)
-   [Tooltip](#tooltip)
-   [TooltipProvider](#tooltipprovider)
-   [API](#api)

</div>
</div>

### Popover

<Popover />

### Tooltip

<Tooltip />

### TooltipProvider

By using the `<TooltipProvider>` it allows you to add the desired tooltip content on each element as a data attribute.

-   `title` is for the tooltip content.
-   `data-tooltip-align` is for alignment direction.
-   `data-tooltip-delay` is for the delay(ms) before showing the tooltip.

<TooltipProvider />

### API

#### Popover

<div>[APITable "clay-popover/src/index.tsx"]</div>

#### Tooltip

<div>[APITable "clay-tooltip/src/Tooltip.tsx"]</div>

#### TooltipProvider

<div>[APITable "clay-tooltip/src/TooltipProvider.tsx"]</div>
