---
title: "Checkbox"
description: "A checkbox is a component that lets the user select the value that is written in its corresponding text label. A user can select multiple checkboxes from a group at the same time."
packageNpm: "@clayui/checkbox"
packageVersion: "3.0.0"
packageStatus: "Beta"
---

import {Checkbox, CheckboxContainerProps, CheckboxIndeterminate, CheckboxesInline, CheckboxLabel, CheckboxWithCallback} from '../../../src/components/clay/Checkbox';

<div class="nav-toc">

- [Best Pratices](#best-pratices)
- [API](#api)

</div>

<div class="clay-site-alert alert alert-info">
	Check the <a href="https://liferay.design/lexicon/">Lexicon</a> <a href="https://liferay.design/lexicon/core-components/forms/radio-check-toggle/">Checkbox Pattern</a> for a more in-depth look at the motivations and proper usage of this component.
</div>

## Best Pratices

Use `checked` property to check or not the checkbox.

<div class="clay-site-alert alert-warning">
    If you want a checkbox without actions on change(or just not use onChange property), use <code class="gatsby-code-text">readOnly</code> property.
</div>

<Checkbox />

Use `indeterminate` property for making the checkbox indeterminate.

<CheckboxIndeterminate />

To set a callback function when the value of the checkbox is changed, use `onChange` property. See the following example:

<CheckboxWithCallback />

If you want to modify the container that wraps the checkbox, use `containerProps`:

<CheckboxContainerProps />

Use `label` property to describe what the checkbox is for.

<CheckboxLabel />

Group checkboxes on the same horizontal row by using `inline` property.

<CheckboxesInline />

## API

<div>
    [APITable "clay-checkbox/src/index.tsx"]
</div>
