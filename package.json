{
  "scripts": {
    "build": "lerna run build",
    "lerna": "lerna bootstrap && npm install",
    "start": "http-server . -p 4000",
    "test": "lerna run test"
  },
  "devDependencies": {
    "http-server": "^0.9.0",
    "lerna": "^2.0.0-rc.5"
  }
}
