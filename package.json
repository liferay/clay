{
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist --nohoist=clay",
    "build": "lerna run build",
    "start": "http-server . -p 4000",
    "test": "lerna run test"
  },
  "devDependencies": {
    "http-server": "^0.9.0",
    "lerna": "2.0.0-beta.37"
  }
}
